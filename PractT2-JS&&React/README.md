# Завдання для виконання

# Номер в журналі: 3

## Структура розділена по файлах

### Рівень 1 — прогноз погоди

•       Створіть файл HTML з елементом <script>.
•  Використовуйте fetch для отримання даних про погоду з API (наприклад, OpenWeatherMap).
•       Розберіть JSON-файл з відповіддю API.
•       Відобразіть поточну температуру та опис погоди на веб-сторінці.

### Рівень 2 — генератор JSON

3.  Генератор JSON
• Напишіть програму, яка генерує JSON-файл з заданими даними.
• Використовуйте вбудовані функції мови програмування для створення JSON-структури.
• Збережіть JSON-файл на диск.

### Рівні 3-4 — веб-додаток для фільмів

3.Створіть веб-додаток для відстеження фільмів. Додайте можливість додавання нових фільмів та їхню рецензію.
для 4р.
3. Додайте різні типи візуалізації (графіки, діаграми, таблиці) ваших даних